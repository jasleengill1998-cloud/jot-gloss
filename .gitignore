.vercel
generated/
node_modules/
dist/

# Audio files (self-hosted MP3s, keep .gitkeep only)
public/audio/*.mp3

# Queue files — tracked so Perplexity can push via git
!queue/staging.txt
!queue/target.txt
!queue/next-task.txt
